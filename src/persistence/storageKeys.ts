/** Canonical localStorage key names, gathered in one place to avoid typo'd duplicates. */
export const STORAGE_KEYS = {
  historySessions: 'grocerymate:history-sessions',
  groceryItems: 'grocerymate:grocery-items',
} as const
